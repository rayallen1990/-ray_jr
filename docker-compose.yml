version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: ray_jr_postgres
    environment:
      POSTGRES_DB: ray_jr
      POSTGRES_USER: ray_jr_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ray_jr_user -d ray_jr"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ray_jr_network

  qdrant:
    image: qdrant/qdrant:latest
    container_name: ray_jr_qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage
    environment:
      QDRANT__SERVICE__GRPC_PORT: 6334
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6333/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ray_jr_network

  redis:
    image: redis:7-alpine
    container_name: ray_jr_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ray_jr_network

volumes:
  postgres_data:
    driver: local
  qdrant_data:
    driver: local
  redis_data:
    driver: local

networks:
  ray_jr_network:
    driver: bridge
